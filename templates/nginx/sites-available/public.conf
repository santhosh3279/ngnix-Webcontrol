server {
    listen 443 ssl;
    server_name public.__DOMAIN__;
    include /etc/nginx/snippets/tls.conf;
    location / {
        include /etc/nginx/snippets/proxy.conf;
        set $backend http://demo:80;
        proxy_pass $backend;
    }
}
